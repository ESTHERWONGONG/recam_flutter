name: recam_flutter
description: ReCam - AI powered analog camera.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- UI 图标库 ---
  cupertino_icons: ^1.0.8

  # --- 你的核心 Swift 插件 (本地路径) ---
  recam_native_camera:
    path: recam_native_camera

  # --- 本地存储 (记账本 & 配置) ---
  shared_preferences: ^2.2.2

  # --- 时间格式化 ---
  intl: ^0.19.0

  # --- 权限管理专家 ---
  permission_handler: ^11.0.1

  # --- ✅ [v1.5 新增] 外部跳转 (跳小红书/浏览器/邮箱) ---
  url_launcher: ^6.2.4

  # --- ✅ [v1.5 新增] 获取 App 版本号 ---
  package_info_plus: ^5.0.1

  # --- 备用相机库 ---
  camera: ^0.10.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true